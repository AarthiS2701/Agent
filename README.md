### Directory Structure

```text
app/
├── gmail/
├── youtube/
├── templates/
│   └── index.html
└── __init__.py

Functions & Features by Component:

app/gmail/
Handles Gmail API integration, OAuth authentication, automated drafting, and sending emails.

app/youtube/
Manages YouTube video searches, metadata retrieval, audio stream processing, and playback.

app/templates/
Renders frontend user interface views using HTML templates like index.html.

app/__init__.py
Initializes the app package and configures backend server routing.
